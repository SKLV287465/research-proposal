\documentclass[../methodology.tex]{subfiles}

\begin{document}
\begin{tikzpicture}[
    scale=0.65,
    transform shape,
    node distance=1.2cm and 1.5cm,
    %% Base module style
    module/.style={
        rectangle, draw,
        minimum width=3.0cm,
        minimum height=0.95cm,
        align=center,
        font=\sffamily\small,
        drop shadow
    },
    %% Client PD
    client/.style={
        module,
        fill=blue!12, draw=blue!55,
        rounded corners=2pt
    },
    %% Pager components
    pagercomp/.style={
        module,
        fill=red!10, draw=red!55,
        rounded corners=2pt
    },
    %% Memory manager components
    memcomp/.style={
        module,
        fill=orange!12, draw=orange!60,
        rounded corners=2pt
    },
    %% Linked list node
    listnode/.style={
        rectangle,
        draw=purple!65,
        fill=purple!12,
        minimum width=1.05cm,
        minimum height=0.65cm,
        font=\sffamily\tiny,
        align=center,
        rounded corners=1pt
    },
    %% Block / storage driver
    driver/.style={
        module,
        fill=green!12, draw=green!55,
        rounded corners=2pt
    },
    %% Hardware / kernel bar
    hw/.style={
        rectangle,
        draw=gray!65,
        fill=gray!22,
        minimum width=23cm,
        minimum height=1.0cm,
        align=center,
        font=\sffamily\small\bfseries,
        drop shadow
    },
    %% Cap / register box
    capbox/.style={
        rectangle,
        draw=teal!60,
        fill=teal!8,
        minimum width=2.4cm,
        minimum height=0.75cm,
        font=\sffamily\tiny,
        align=center,
        rounded corners=1pt
    },
    %% IPC endpoint badge
    ipc/.style={
        ellipse,
        draw=violet!60,
        fill=violet!10,
        minimum width=1.8cm,
        minimum height=0.6cm,
        font=\sffamily\tiny,
        align=center
    },
    %% Arrows
    arrow/.style={
        -{Stealth[scale=1.25]}, thick
    },
    darrow/.style={
        {Stealth[scale=1.25]}-{Stealth[scale=1.25]}, thick
    },
    dasharrow/.style={
        -{Stealth[scale=1.25]}, thick, dashed
    },
    dashdoublearrow/.style={
        {Stealth[scale=1.25]}-{Stealth[scale=1.25]}, thick, dashed
    },
    %% Annotation label  *** FIX: added align=center so \\ works ***
    annot/.style={
        font=\sffamily\tiny,
        text=black!70,
        align=center
    }
]

% ============================================================
% ROW 0 — CLIENT PROTECTION DOMAINS
% ============================================================
\node[client] (client1) at (0, 0)
    {Client PD 1\\{\tiny heap: 0x8000000000}};
\node[client] (client2) at (4.5, 0)
    {Client PD 2\\{\tiny heap: 0x8000000000}};
\node[font=\Large\sffamily] (ellipsis) at (8.2, 0) {\ldots};
\node[client] (client3) at (11.5, 0)
    {Client PD N\\{\tiny heap: 0x8000000000}};

% VSpace / capability annotations below each client
\node[capbox] (vcap1) at (0,    -1.15)
    {VSpace Cap\\{\tiny root\_cnode[0]}};
\node[capbox] (vcap2) at (4.5,  -1.15)
    {VSpace Cap\\{\tiny root\_cnode[0]}};
\node[capbox] (vcap3) at (11.5, -1.15)
    {VSpace Cap\\{\tiny root\_cnode[0]}};

% IPC endpoints between clients and pager
\node[ipc] (ep1) at (0,   -2.4) {EP: fault};
\node[ipc] (ep2) at (4.5, -2.4) {EP: fault};
\node[ipc] (ep3) at (11.5,-2.4) {EP: fault};

\draw[arrow, blue!50, thin] (client1.south) -- (vcap1.north);
\draw[arrow, blue!50, thin] (client2.south) -- (vcap2.north);
\draw[arrow, blue!50, thin] (client3.south) -- (vcap3.north);

\draw[arrow, violet!55, thin] (vcap1.south) -- (ep1.north);
\draw[arrow, violet!55, thin] (vcap2.south) -- (ep2.north);
\draw[arrow, violet!55, thin] (vcap3.south) -- (ep3.north);

% ============================================================
% ROW 1 — PAGER COMPONENTS (core pipeline)
% ============================================================
\node[pagercomp] (fault) at (0,   -4.5)
    {Fault Handler\\{\tiny fault()}};
\node[pagercomp] (pt)    at (4.5, -4.5)
    {Page Table\\{\tiny page\_table[PD][VPN]}};
\node[pagercomp] (lru)   at (9.0, -4.5)
    {Two-List LRU\\{\tiny get\_frame() / pin()}};

% Clock / reference bit note
\node[annot] at (9.0, -3.8)
    {\textit{clock ref bit tracked}};

% ============================================================
% ROW 2 — PAGER COMPONENTS (I/O pipeline)
% ============================================================
\node[pagercomp] (pageio)   at (0,   -7.2)
    {Page I/O\\{\tiny page\_in() / page\_out()}};
\node[pagercomp] (cont)     at (4.5, -7.2)
    {Continuations\\{\tiny saved\_reply\_cap[]}};
\node[pagercomp] (swapmeta) at (9.0, -7.2)
    {Swap Metadata\\{\tiny slot\_index / dirty bit}};

% ============================================================
% ROW 3 — PAGER NOTIFY + STATS
% ============================================================
\node[pagercomp] (notified) at (0,   -9.8)
    {Notify Handler\\{\tiny notified()}};
\node[pagercomp] (stats)    at (4.5, -9.8)
    {Pager Stats\\{\tiny faults / evictions / I/Os}};

% ============================================================
% TWO-LIST LRU INTERNALS
% ============================================================

%% Active list
\node[font=\sffamily\tiny\bfseries, text=purple!75]
    (activelabel) at (13.1, -4.5) {Active List};
\node[listnode] (active_head) at (14.2, -4.5) {Head};
\node[listnode] (active_n1)   at (15.4, -4.5) {Frm\\{\tiny ref=1}};
\node[listnode] (active_n2)   at (16.6, -4.5) {Frm\\{\tiny ref=0}};
\node[listnode] (active_tail) at (17.8, -4.5) {Tail};

%% Inactive list
\node[font=\sffamily\tiny\bfseries, text=purple!75]
    (inactivelabel) at (13.1, -6.0) {Inactive List};
\node[listnode] (inactive_head) at (14.2, -6.0) {Head};
\node[listnode] (inactive_n1)   at (15.4, -6.0) {Frm\\{\tiny dirty}};
\node[listnode] (inactive_n2)   at (16.6, -6.0) {Frm\\{\tiny clean}};
\node[listnode] (inactive_tail) at (17.8, -6.0) {Tail};

%% Free list
\node[font=\sffamily\tiny\bfseries, text=purple!75]
    (freelabel) at (13.1, -7.5) {Free List};
\node[listnode] (free_head) at (14.2, -7.5) {Head};
\node[listnode] (free_n1)   at (15.4, -7.5) {Frm};
\node[listnode] (free_n2)   at (16.6, -7.5) {Frm};
\node[listnode] (free_tail) at (17.8, -7.5) {Tail};

%% Intra-list arrows
\draw[arrow, purple!60] (active_head)   -- (active_n1);
\draw[arrow, purple!60] (active_n1)     -- (active_n2);
\draw[arrow, purple!60] (active_n2)     -- (active_tail);
\draw[arrow, purple!60] (inactive_head) -- (inactive_n1);
\draw[arrow, purple!60] (inactive_n1)   -- (inactive_n2);
\draw[arrow, purple!60] (inactive_n2)   -- (inactive_tail);
\draw[arrow, purple!60] (free_head)     -- (free_n1);
\draw[arrow, purple!60] (free_n1)       -- (free_n2);
\draw[arrow, purple!60] (free_n2)       -- (free_tail);

%% Promote (inactive -> active)
\draw[arrow, bend left=35, orange!75, thick]
    (inactive_n1.north)
    to node[right, annot] {promote\_page()}
    (active_n1.south);

%% Demote (active -> inactive)
\draw[arrow, bend right=35, blue!55, thick]
    (active_n2.south)
    to node[left, annot] {demote\_page()}
    (inactive_n2.north);

%% Evict (inactive tail -> free)
\draw[arrow, gray!65, thick]
    (inactive_tail.east)
    to[out=0, in=0]
    node[right, annot] {free\_frame()\\evict\_page()}
    (free_tail.east);

%% LRU -> active head
\draw[arrow, purple!60]
    (lru.east)
    -- node[above, annot] {manages}
    (active_head.west);

% ============================================================
% MEMORY MANAGER PD
% ============================================================
\node[memcomp] (mm)        at (20.0, -4.5)
    {Memory Manager\\{\tiny malloc() / free()}};
\node[memcomp] (mmfreelist) at (20.0, -6.2)
    {Free List Allocator\\{\tiny O(1) linked list}};
\node[memcomp] (mmframe)   at (20.0, -7.9)
    {Frame Pool\\{\tiny base: 0x8000000000}};
\node[memcomp] (mmcap)     at (20.0, -9.6)
    {Cap Allocation\\{\tiny untyped / retype}};

%% MM internals
\draw[arrow, orange!60] (mm.south)        -- (mmfreelist.north);
\draw[arrow, orange!60] (mmfreelist.south) -- (mmframe.north);
\draw[arrow, orange!60] (mmframe.south)   -- (mmcap.north);

% Annotation: frame size
\node[annot] at (20.0, -8.7) {\textit{4 KiB pages, 4 GiB pool}};

% ============================================================
% BLOCK DRIVER PD
% ============================================================
\node[driver]  (blk)       at (0,   -12.5)
    {Block Driver\\{\tiny sddf/blk}};
\node[driver]  (blkqueue)  at (4.5, -12.5)
    {BLK Request Queue\\{\tiny blk\_queue\_handle\_t}};
\node[driver]  (blkresp)   at (9.0, -12.5)
    {BLK Response Queue\\{\tiny completion ring}};
\node[driver]  (storage)   at (0,   -14.5)
    {Storage Device\\{\tiny Block Device / NVMe}};
\node[driver]  (dma)       at (4.5, -14.5)
    {DMA Region\\{\tiny shared phys mem}};

%% Block driver internals
\draw[darrow, green!60]  (blk.east)      -- (blkqueue.west);
\draw[darrow, green!60]  (blkqueue.east) -- (blkresp.west);
\draw[arrow,  green!60]  (blk.south)     -- (storage.north);
\draw[arrow,  green!60]  (blkqueue.south)-- (dma.north);

% ============================================================
% MICROKERNEL BAR
% ============================================================
\node[hw] (kernel) at (9.5, -17.0)
    {seL4 Microkernel (Microkit) \quad --- \quad
     IPC \;|\; Capabilities \;|\; VSpace Management \;|\;
     Fault Delegation \;|\; Untyped Memory \;|\; Notifications};

% ============================================================
% BACKGROUND BOXES
% ============================================================
\begin{scope}[on background layer]

    %% Pager PD box
    \node[
        fill=red!4, draw=red!45, dashed, rounded corners=4pt,
        inner sep=0.4cm,
        fit=(fault)(pt)(lru)(pageio)(cont)(swapmeta)
            (notified)(stats)
            (active_head)(active_tail)(activelabel)
            (inactive_head)(inactive_tail)(inactivelabel)
            (free_head)(free_tail)(freelabel)
    ] (pagerbox) {};

    %% Memory Manager PD box
    \node[
        fill=orange!4, draw=orange!45, dashed, rounded corners=4pt,
        inner sep=0.4cm,
        fit=(mm)(mmfreelist)(mmframe)(mmcap)
    ] (mmbox) {};

    %% Block Driver PD box
    \node[
        fill=green!4, draw=green!45, dashed, rounded corners=4pt,
        inner sep=0.4cm,
        fit=(blk)(blkqueue)(blkresp)(storage)(dma)
    ] (blkbox) {};

    %% Client group box
    \node[
        fill=blue!4, draw=blue!35, dashed, rounded corners=4pt,
        inner sep=0.4cm,
        fit=(client1)(client2)(client3)(ellipsis)
            (vcap1)(vcap2)(vcap3)(ep1)(ep2)(ep3)
    ] (clientbox) {};

    %% Overall User Space box
    \node[
        fill=blue!2, draw=blue!25, dashed, rounded corners=6pt,
        inner sep=0.6cm,
        fit=(clientbox)(pagerbox)(mmbox)(blkbox)
    ] (userbox) {};

\end{scope}

% ============================================================
% BOX LABELS
% ============================================================
\node[anchor=north west,
      font=\sffamily\small\bfseries, text=blue!65]
    at (userbox.north west)  {User Space};

\node[anchor=north west,
      font=\sffamily\small\bfseries, text=blue!65]
    at (clientbox.north west) {Client Protection Domains};

\node[anchor=north west,
      font=\sffamily\small\bfseries, text=red!65]
    at (pagerbox.north west)  {Pager PD};

\node[anchor=north west,
      font=\sffamily\small\bfseries, text=orange!65]
    at (mmbox.north west)     {Memory Manager PD};

\node[anchor=north west,
      font=\sffamily\small\bfseries, text=green!65]
    at (blkbox.north west)    {Block Driver PD};

% ============================================================
% INTER-PD CONNECTIONS
% ============================================================

%% Fault endpoints -> Fault Handler
\draw[arrow, violet!60]
    (ep1.south)
    -- node[left, annot] {VM Fault\\(IPC)}
    (fault.north);
\draw[dasharrow, violet!50] (ep2.south) -- ++(0,-1.0) -| (fault.north);
\draw[dasharrow, violet!50] (ep3.south) -- ++(0,-1.0) -| (fault.north);

%% Clients -> Memory Manager (PPC)
\draw[darrow, orange!65]
    (client1.east)
    -- ++(1.5,0) -- ++(0,4.5)
    -| node[above, annot, near end] {PPC: malloc/free}
    (mm.north);

%% Fault Handler -> Page Table
\draw[darrow, red!60]
    (fault.east)
    -- node[above, annot] {lookup / update}
    (pt.west);

%% Page Table -> LRU
\draw[darrow, red!60]
    (pt.east)
    -- node[above, annot] {get\_frame()}
    (lru.west);

%% Fault Handler -> Page I/O
\draw[arrow, red!55]
    (fault.south)
    -- node[left, annot] {evict / load}
    (pageio.north);

%% Page I/O -> Continuations
\draw[darrow, red!55]
    (pageio.east)
    -- node[above, annot] {save reply cap}
    (cont.west);

%% Page I/O -> Swap Metadata
\draw[darrow, red!55]
    (cont.east)
    -- node[above, annot] {slot / dirty}
    (swapmeta.west);

%% Page I/O -> Notify Handler (async)
\draw[arrow, red!55]
    (pageio.south)
    -- node[left, annot] {async I/O req}
    (notified.north);

%% Notify Handler -> Stats
\draw[arrow, red!55]
    (notified.east)
    -- node[above, annot] {update counters}
    (stats.west);

%% Notify Handler -> Block Driver (notification channel)
\draw[darrow, green!60]
    (notified.south)
    -- node[left, annot] {enqueue\_req()\\microkit\_notify()}
    (blk.north);

%% Block Response -> Notify Handler (completion)
\draw[dasharrow, green!50, bend left=20]
    (blkresp.north)
    to node[right, annot] {IRQ / notify}
    (notified.east);

%% Pager -> Memory Manager (frame alloc)
\draw[darrow, orange!60]
    (lru.north east)
    -- ++(0,0.6) -|
    node[above, annot, near end] {alloc\_frame() PPC}
    (mm.north west);

%% Pager -> Microkernel
\draw[dasharrow, gray!55]
    (pagerbox.south)
    -- ++(0,-0.55)
    -- node[above, annot] {map\_page() / unmap\_page()}
    (kernel.north -| pagerbox.south);

%% MM -> Microkernel
\draw[dasharrow, gray!55]
    (mmbox.south)
    -- ++(0,-6.7)
    -- (kernel.north -| mmbox.south);
\node[annot, rotate=90] at ($(mmbox.south)+(0.35,-3.4)$)
    {untyped\_retype()};

%% Block driver -> Microkernel
\draw[dasharrow, gray!55]
    (blkbox.south)
    -- ++(0,-0.45)
    -- node[above, annot] {DMA caps / IRQ caps}
    (kernel.north -| blkbox.south);

%% seL4 -> kernel note
\node[annot] at (9.5, -16.25)
    {\textit{All cross-PD communication via seL4 IPC / Notifications.
     \quad Physical memory managed through Untyped capabilities.}};

% ============================================================
% LEGEND
% ============================================================
\begin{scope}[shift={(16.5, 1.8)}]
    \node[font=\sffamily\small\bfseries] at (0,0) {Legend};
    \draw[arrow,  blue!55,  thick] (-1.5,-0.55) -- (-0.5,-0.55)
        node[right, annot] {IPC / Fault};
    \draw[darrow, orange!60, thick] (-1.5,-1.1)  -- (-0.5,-1.1)
        node[right, annot] {PPC (Procedure Call)};
    \draw[arrow,  red!55,   thick] (-1.5,-1.65) -- (-0.5,-1.65)
        node[right, annot] {Internal data flow};
    \draw[dasharrow, gray!55, thick] (-1.5,-2.2) -- (-0.5,-2.2)
        node[right, annot] {Syscall / Kernel};
    \draw[arrow,  green!55, thick] (-1.5,-2.75) -- (-0.5,-2.75)
        node[right, annot] {Device I/O};
    \draw[arrow,  purple!60,thick] (-1.5,-3.3)  -- (-0.5,-3.3)
        node[right, annot] {LRU list link};
\end{scope}

\end{tikzpicture}
\end{document}